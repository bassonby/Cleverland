"use strict"

const userAge = +prompt("Сколько вам лет?", );
function checkAge (age){
    if(userAge < 16 ){
        alert("Вы еще слишком молоды для всего этого!")
    }
    else{
        alert("Так значит вы видели как зажигались и потухали звезды")
    }
}

checkAge();